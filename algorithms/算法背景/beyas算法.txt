贝叶斯分类器算法：
	特征之间相关性较小时比较适用，若特征之间相关性较大，则不满足朴素贝叶斯使用的假设（特征之间相互独立），不宜用此算法。
存在问题：
	1、类条件概率，特征独立分开相乘时，有可可能存在有的条件特征概率为0，导致相乘结果的类条件概率整个为0；（解决办法：所有特征出现次数初始化为1，并将分母<类条件下特征出现总数>初始化为2）
	2、下溢出：太多很小的数相乘造成。类条件概率分开相乘时，大部分因子太小，可能出现下溢出或得不到答案。（解决办法：对乘积取自然对数求解，会避免下溢出或浮点舍入导致错误）