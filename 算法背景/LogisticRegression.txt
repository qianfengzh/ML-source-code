一个判断优化算法优劣的有效方法是：
	看它是否收敛，即参数是否达到了稳定值，是否还会不断地变化。
Cast function:最小二乘法

梯度上升算法：
	w = w + alpha * gradient
梯度上升算法是一种‘批处理’算法。Batch

随机梯度上升算法：
（一次仅使用一个样本点来更新回归系数）
梯度上升算法能用新样本对分类器增量式更新，是一种‘在线学习’算法。
	梯度上升算法每次更新回归系数时需遍历整个数据集，若样本和特征数较多，则该方法计算复杂度就会急剧上升。
	